{
  "name": "business-suite-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init:local": "wrangler d1 execute business-suite-db --local --file=src/db/schema.sql",
    "db:init:remote": "wrangler d1 execute business-suite-db --remote --file=src/db/schema.sql"
  },
  "dependencies": {
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240117.0",
    "wrangler": "^3.25.0"
  }
}
